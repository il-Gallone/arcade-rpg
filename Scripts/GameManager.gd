extends Node

var Players = Array()
var Enemies = Array()

var maxEnemies = 100
var enemyWave = 0


var waveModifer: BuffBase
var modiferWaves = 0

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta: float) -> void:
	pass
